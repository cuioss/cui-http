= cui-http Quality Review
:toc: left
:toclevels: 3
:sectnums:
:icons: font

== Purpose and scope

This directory holds the results of a full review of the `cui-http` repository (state: `main` at commit `0bad295`, version `3.1-SNAPSHOT`, 2026-09-05) covering

* all production code of `cui-http-core` (13 packages, 19 174 lines) and `cui-http-benchmarking` (600 lines),
* all tests (46 436 lines: unit tests, attack databases, generators, integration tests),
* all documentation: Javadoc, `README.adoc`, `CLAUDE.md`, `agents.md`, every file under `doc/` including the 15 ADRs, `LogMessages.adoc`, the requirement and specification documents, and
* the build and CI infrastructure (three POMs, eight GitHub workflows, the benchmark publishing script).

The review question set was:

. Is the code correct, including edge cases?
. Are there hidden traps, i.e. a false feeling of security where the protection is not actually there?
. Is the claimed security posture fully implemented, in particular "fail closed"?
. Are the tests real content tests, or coverage padding? Are positive and negative cases present? Are the cui-test-generator generators used correctly?
. Is the documentation (Javadoc and AsciiDoc) sensibly structured and does it represent the current code?
. Consistency, completeness, ambiguity, duplication across code and documentation.

Nothing in the repository other than this directory was changed. Every finding is a report item; no fixes were applied.

== How the review was conducted

. *Baseline.* The full core test suite was executed first: 112 surefire report files covering 206 test classes (nested classes included), 7 874 tests, 0 failures, 0 errors, 0 skipped.
. *Independent reading of the security core* by the lead reviewer: every validation stage, every pipeline, the configuration presets, the forwarded-header resolver, the TLS provider, the redirect policy and the redirect follow loop were read in full.
. *Six parallel area reviews*, each reading its files in full and each required to cite `path:line` evidence for every finding and to label each finding `CONFIRMED` (traced line by line or executed) or `SUSPECTED`.
. *Empirical probes.* Each area reviewer and the lead compiled small Java or jshell programs against the freshly built `target/classes` and ran concrete inputs (traversal spellings, encodings, header injections, forwarded-header spoofing, redirect chains, cache-key collisions) through the real pipelines. Where an output is quoted in a finding it was produced by such a run.
. *Adversarial verification.* After the initial reports were consolidated, an independent reviewer with no prior context re-verified every finding end to end (see xref:07-adversarial-verification.adoc[]). It reproduced all of them; its corrections (line numbers, five factual details, five severities, the baseline numbers) were applied in place and are marked as such, and the one item it found that the review had missed was added as V-1.

Reviewers were told not to pad, to report only specific and actionable items, and to list what they verified as correct so the report is not read as "everything is broken".

== Baseline metrics

Captured on the reviewed commit with the project's own build, before any review work started, and re-captured after the review to confirm the figures (the coverage numbers below were first recorded by a parallel review skeleton that has been merged into this branch, and were re-verified by a fresh `verify -Pcoverage` run).

[cols="2,1",options="header"]
|===
| Metric | Value

| Tests executed (`cui-http-core`) | 7 874
| Failures / errors / skipped | 0 / 0 / 0
| Test classes (surefire, nested classes included) / report files | 206 / 112
| Line coverage (JaCoCo, `verify -Pcoverage`) | 95.0 % (2 598 of 2 734)
| Branch coverage | 91.8 % (1 483 of 1 616)
| Production Java files / lines (`cui-http-core`) | 72 / 19 174
| Test Java files / lines | 171 / 46 436
| Benchmark module Java lines | 600
| AsciiDoc files / lines under `doc/` (excluding this report) | 36 / about 8 500
|===

.Coverage per package (line %, branch %)
[cols="3,1,1",options="header"]
|===
| Package | Line | Branch

| `de.cuioss.http.client` | 100.0 | 100.0
| `de.cuioss.http.client.adapter` | 87.8 | 90.7
| `de.cuioss.http.client.converter` | 100.0 | 80.0
| `de.cuioss.http.client.handler` | 94.6 | 95.3
| `de.cuioss.http.client.result` | 100.0 | 88.5
| `de.cuioss.http.forwarded` | 97.3 | 94.3
| `de.cuioss.http.security.config` | 94.6 | 79.7
| `de.cuioss.http.security.core` | 100.0 | 100.0
| `de.cuioss.http.security.data` | 99.3 | 92.9
| `de.cuioss.http.security.exceptions` | 100.0 | 83.3
| `de.cuioss.http.security.monitoring` | 100.0 | 100.0
| `de.cuioss.http.security.pipeline` | 98.9 | 94.1
| `de.cuioss.http.security.validation` | 94.5 | 88.8
|===

Classes below the project's own 80 % target on at least one axis: `SecurityConfigurationBuilder` (branch 73.5 %), `NormalizationStage` (branch 78.8 %), `HostnameVerificationRelaxingTrustManager` (line 54.2 %; the `Socket`-typed overloads are never exercised because the JDK HTTP client only calls the `SSLEngine` overloads), and one uncovered branch each in `HttpAdapter`, `HttpResult.Success` and `ForwardedHeaderResolver.HostPort`. Coverage is high throughout; the test-quality findings in this report are about what the tests assert, not about which lines they execute.

== Limitations

* The review is of a snapshot (`0bad295`). Line references were re-verified against that commit during the adversarial verification; they will drift with the next edit of the referenced file.
* Network-dependent behaviour was exercised only against in-process servers (MockWebServer, a one-shot TLS server); no public host was contacted by the review itself.
* Reviewers worked independently, so the same defect can appear under two identifiers; the consolidation tables in each chapter name the primary record.
* The benchmarking module was reviewed for correctness and consistency of what it measures, not for the validity of the published numbers.
* The canonical build executor `.plan/execute-script.py` is not part of the repository and was absent in the review environment; the baseline, the coverage run and the pre-commit gate used the Maven wrapper directly.

== Severity scale

[cols="1,4",options="header"]
|===
| Severity | Meaning used in this report

| Critical | Exploitable by an unauthenticated remote party against a default configuration with direct security impact. None found.
| High | A security or correctness defect a reasonable integrator following the documentation would run into, or a documented protection that is not delivered.
| Medium | A gap, false-security trap or correctness defect that needs a specific (non-default) configuration or usage pattern, or a documentation statement that materially misdescribes behaviour.
| Low | Correctness, consistency or test-quality issue with limited or no security consequence.
| Info | Observation, drift, or a recommendation without a defect.
|===

Finding identifiers keep the prefix of the review stream that produced them so every item can be traced to its raw evidence and to the adversarial verification:

[cols="1,3",options="header"]
|===
| Prefix | Stream

| `L-` | Lead reviewer (independent read of the security core plus three compiled probes)
| `V-` | Added by the adversarial verification
| `F-A-` | Validation stages and configuration
| `F-B-` | Pipelines, data classes, exceptions, monitoring
| `F-client-` | HTTP client: TLS, redirects, retry, ETag cache, result types
| `F-D-` | Forwarded-header resolution, benchmarking module, build and CI
| `F-E-` | Security test framework: generators, attack databases, integration tests
| `F-F-` | Documentation set
|===

== Executive summary

The library is in better shape than a list of 126 findings suggests. The central security mechanisms were probed hard and held: no path-traversal bypass of the default configuration was found across roughly a hundred encoded, double-encoded, overlong, Unicode-folded, semicolon-parameter and backslash spellings; header pipelines reject CR/LF under every preset; the TLS floor is really pinned on the wire and there is no trust-all path; redirects refuse downgrades before consulting the allow-list; the forwarded-header resolver's CIDR arithmetic, nearest-hop selection and three-state `Forwarded` model are correct; events are counted exactly once and shared pipelines are thread-safe.

The findings cluster around four themes.

. *Protections that are promised but not delivered as stated.* The double-encoding gate recognises only the literal spelling `%25XX`, so `%25%32%66` passes every preset and the pipeline hands back `%2f` (F-A-1). The decoded-delimiter rule for parameter names runs before Unicode normalisation, so U+037E becomes `;` in the returned name (F-A-2). "Malformed UTF-8 is rejected" is true only for a regex list; everything else becomes U+FFFD and is accepted (F-A-4). The content-type pipeline has no length or character stage and its list entries are not normalised like the values (L-3, F-B-1, F-B-2). The cookie prefix stage validates prefixes and nothing else, case-sensitively (L-5, F-A-3, F-B-6). A segment-terminal `..`, with or without a decoded `?`/`#` glued to it, is accepted and the returned value may still carry the dot-dot (L-1, F-A-8, V-1).
. *Trust-model traps in the forwarded-header resolver.* The two de-facto header families are not reconciled against each other, so whichever family the operator's proxy does not write is attacker-controlled (F-D-1); a single bogus `Forwarded` header from a client blanks every resolved field, and all three documented consumers then fall back to `http` (F-D-2); the socket peer is never an input, so `trustedProxies` cannot gate whether headers are believed at all (F-D-3, documented as a deployment precondition).
. *A cross-principal data leak in the ETag cache* under the cache-key configuration the Javadoc itself recommends for token refresh: a 4xx for user B is answered with user A's cached body as "fallback content" (F-client-1).
. *Test and documentation claims that outrun the code.* The header-injection, request-smuggling and URL-length test classes are decided on a literal `?` before the mechanism under test runs (F-E-1); several "legitimate input" tests cannot fail (F-E-2); most attack generators are fixed lists sampled with a fresh random seed so entries are skipped on most runs (F-E-5); log-coverage, test-count, CWE and requirement-traceability statements are stale or unsupported (F-documentation-4, -6, -7, -14, -15); the "Security Integration" samples in two `package-info` files do not compile and invert the fail-secure contract (F-documentation-2).

No Critical finding was recorded: every High item either needs a specific non-default configuration or deployment shape, or is caught by a later stage for the traversal-shaped variant. Three items originally rated High (F-E-1, F-E-2, F-documentation-2) were moved to Medium after the adversarial verification because they are test or documentation defects rather than runtime ones.

=== Findings by severity and stream

[cols="3,1,1,1,1,1,1",options="header"]
|===
| Stream | Total | Critical | High | Medium | Low | Info

| Lead (`L-`) | 13 | 0 | 0 | 6 | 6 | 1
| Added by adversarial verification (`V-`) | 1 | 0 | 0 | 1 | 0 | 0
| Validation stages and configuration (`F-A-`) | 14 | 0 | 2 | 7 | 4 | 1
| Pipelines, data, exceptions, monitoring (`F-B-`) | 16 | 0 | 0 | 7 | 7 | 2
| HTTP client (`F-client-`) | 18 | 0 | 1 | 5 | 9 | 3
| Forwarded headers, build, CI (`F-D-`) | 20 | 0 | 2 | 4 | 10 | 4
| Security test framework (`F-E-`) | 16 | 0 | 0 | 9 | 6 | 1
| Documentation (`F-F-`) | 28 | 0 | 0 | 8 | 17 | 3
| *All* | *126* | *0* | *5* | *47* | *59* | *15*
|===

Several defects were found by more than one stream; the per-document "Consolidation notes" list the duplicates and name the primary record. Counting duplicates once, the number of distinct defects is about 110. All severities and counts are as they stand after the adversarial verification.

=== The five High findings

[cols="1,4,2",options="header"]
|===
| ID | Finding | Where

| F-A-1 | Double-encoding gate bypassed by percent-encoding the hex digits; `%25%32%66` returns `%2f`, `%25%30%30` returns `%00`, under every preset. | xref:01-security-validation.adoc[]
| F-A-2 | Parameter-name delimiter check runs before Unicode normalisation; `a%CD%BEb` (U+037E) returns `a;b` from the parameter-name pipeline. | xref:01-security-validation.adoc[]
| F-client-1 | ETag adapter serves one principal's cached body to another as 4xx "fallback content" under the recommended `CacheKeyHeaderFilter.excluding("Authorization")`. | xref:03-http-client.adoc[]
| F-D-1 | `X-Forwarded-*` and NiFi `X-Proxy*` are never reconciled; the family the proxy does not write is attacker-winnable. | xref:02-forwarded-header-resolution.adoc[]
| F-D-2 | Any client can null every resolved forwarded field with one malformed `Forwarded` header; documented consumers then default the scheme to `http`. | xref:02-forwarded-header-resolution.adoc[]
|===

The most important Medium items on the test and documentation side are F-E-1 and F-E-2 (attack tests decided on a literal `?` before the mechanism runs; tests that accept either outcome) and F-documentation-2 (non-compiling, contract-inverting integration samples); all three were proposed as High and recorded as Medium after the adversarial verification.

=== Answers to the review questions

Is the code correct, including edge cases?::
Largely yes for the core paths; the edge-case defects are concentrated in decoding (F-A-1, F-A-2, F-A-4), the terminal `..` clamp and its decoded-`?` variant (L-1, F-A-8, V-1), cookie attribute semantics (F-B-8), redirect method rewriting on 301/302 (F-client-3), the `SSLHandshakeException` retry classification (F-client-4) and host/port reconciliation in the forwarded resolver (F-D-4, F-D-5).

Are there hidden traps, a false feeling of security?::
Yes, and they are the most important output of this review: the content-type pipeline that enforces nothing by default and cannot see CR/LF after a `;` (L-3, F-B-1, F-B-2); `validateCookie` that validates nothing about the cookie's content (L-5, F-B-6); `trustedProxies` that never gates trust (F-D-3); `excluding("Authorization")` sold as "maintaining security" (F-client-1); `failOnSuspiciousPatterns=false` by default with no observability (ADR-0006, F-E-4); factory Javadoc promising XSS and "malicious header content" detection that no stage performs (F-B-3); requirement documents marked VERIFIED for properties the library does not provide (F-documentation-15).

Is the claimed security posture fully implemented, always fail closed?::
Fail-closed is real where it was designed in (thrown exceptions, counted events, unresolvable `Forwarded` state, no trust-all TLS path). It is not real for malformed UTF-8 (silent replacement, F-A-4), for the double-encoding gate (F-A-1), for `null` Content-Type against an allow-list (F-B-2), and it is attacker-triggerable in the wrong direction for the forwarded scheme (F-D-2). `when()`/`identity()` are fail-open by design and documented as such (ADR-0013, verified).

Are the tests real content tests with positive and negative cases?::
The attack-database tests, the pipeline regression tests, the redirect and TLS integration tests and the forwarded trust-boundary tests are strong and assert exact verdicts. The `tests/` integration layer for header injection, smuggling and length is not testing what its names say (F-E-1), seven methods are vacuous (F-E-2), eleven helpers accept 6 to 10 of 24 failure types (F-E-3), and roughly forty `ETagAwareHttpAdapterTest` methods end in `assertNotNull` while firing real requests at `api.example.com` (F-client-7). The per-area inventories list, for every production class, whether positive and negative cases exist.

Are generators used correctly?::
Partly. The traversal, encoding and Unicode generators are genuinely combinatorial and come with contract tests. Most other "generators" wrap fixed literal lists that are sampled, not iterated, with a per-run random seed (F-E-5); valid generators stay inside the smallest accept set and cannot reveal false positives (F-E-6); five traversal generators overlap (F-E-7); eleven generators have no contract test (F-E-15); six of nine stage tests and the whole client and forwarded suites use no cui-test-generator at all (F-A-13, F-client-18, F-D-19).

Is the documentation sensibly structured and current?::
The structure is good and almost every link, anchor, class name, numeric default and stage order was verified correct. The defects are concentrated in code samples that do not compile or teach the wrong usage (F-documentation-2, -3, -16, F-B-12), in coverage and count claims (F-documentation-4, -7), in requirement and traceability documents that assert unimplemented properties (F-documentation-14, -15), in the pipeline-selection matrix routing host/IPv6 concerns to a pipeline that only character-checks them (F-documentation-10), in all fifteen ADRs still carrying `Status: Proposed`, and in an orphaned ADR index (F-documentation-12).

== Documents in this directory

xref:01-security-validation.adoc[Security Validation: Stages, Pipelines and Configuration]::
`de.cuioss.http.security.*`: lead findings, the item added by the adversarial verification, stream A (stages, configuration, core), stream B (pipelines, data classes, exceptions, monitoring). 44 items.

xref:02-forwarded-header-resolution.adoc[Forwarded-Header Resolution]::
`de.cuioss.http.forwarded`: trust model, reconciliation, parsing, CIDR handling. 15 items.

xref:03-http-client.adoc[HTTP Client]::
`de.cuioss.http.client.*`: TLS, redirects, retry, ETag cache, result types, converters, log messages. 18 items.

xref:04-test-framework.adoc[Security Test Framework]::
Generators, attack databases, integration tests and the documents that describe them, with a validator-to-test matrix, a generator inventory with draw counts and an attack-database inventory. 16 items.

xref:05-documentation.adoc[Documentation]::
Every AsciiDoc and Markdown file, all `package-info.java` files, class-level Javadoc, `LogMessages.adoc` (full identifier table), requirement and traceability spot-check of 31 rows. 28 items.

xref:06-build-and-infrastructure.adoc[Build, CI and Benchmarking]::
POMs, workflows, benchmark module and publishing script. 5 items.

xref:07-adversarial-verification.adoc[Adversarial Verification]::
Independent end-to-end re-verification of all 125 original findings by a reviewer with no prior context: 99 confirmed exactly, 22 confirmed with a correction, 2 confirmed on spot-check, 1 confirmed as suspected, 1 confirmed and superseded, none refuted; 38 strength claims re-executed, none false; one missed item (V-1). Lists the adjustments made to this report as a result, followed by the verifier's report verbatim.

== Suggested order of remediation

. F-A-1, F-A-2, F-A-4, V-1 and L-1/F-A-8 together: replace the wire-form regexes in `DecodingStage` with a real UTF-8 decode that reports malformed input, decide double encoding on the decoded output, run the decoded-character checks on the normalised string and reject decoded `?`/`#` in paths, and treat any segment starting with `..` as traversal intent.
. F-client-1: restrict fallback content to availability failures and make credential-bearing headers part of the cache-entry identity; rewrite the token-refresh guidance.
. F-D-1, F-D-2, F-D-4: reconcile all three header families, document that the ingress proxy must strip the families it does not write, change the `orElse("http")` examples, reconcile host and port separately.
. L-3, F-B-1, F-B-2: give the content-type pipeline length and character stages, normalise list entries at construction, document `null` semantics.
. F-E-1, F-E-2, F-E-3, F-E-5: point header-injection and smuggling payloads at the surfaces that receive them, give every test an expected verdict, assert exact failure types, iterate fixed lists deterministically.
. F-documentation-2, -3, -16 and F-B-12: make every Javadoc sample compile against the real API and use the right pipeline for names and values.
. The remaining Medium items, then Low and Info, in the order listed in each document.
