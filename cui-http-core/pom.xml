<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.cuioss</groupId>
        <artifactId>cui-http-parent</artifactId>
        <version>2.2-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>cui-http</artifactId>
    <packaging>jar</packaging>
    <name>CUI HTTP</name>
    <description>Provides a number of http-related utilities.</description>
    <properties>
        <maven.jar.plugin.automatic.module.name>de.cuioss.http</maven.jar.plugin.automatic.module.name>
        <!-- Single authoritative definition of the packages the 'generators' classifier ships.
             Both the maven-jar-plugin execution that builds the classifier and the
             maven-enforcer-plugin execution that guards it reference these, so the two cannot
             drift: adding a package here extends the artifact AND the guard together. Paths are
             relative to ${project.build.testOutputDirectory}. -->
        <generators.classifier.package.generators>de/cuioss/http/security/generators</generators.classifier.package.generators>
        <generators.classifier.package.database>de/cuioss/http/security/database</generators.classifier.package.database>
    </properties>
    <dependencies>
        <dependency>
            <groupId>de.cuioss</groupId>
            <artifactId>cui-java-tools</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-generator</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Unit testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-mockwebserver-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- Controls certificate SAN for the TLS hostname-verification regression test (F-19).
                 Version aligned with the transitive okhttp brought in by cui-test-mockwebserver-junit5. -->
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp-tls</artifactId>
            <version>5.5.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-value-objects</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-juli-logger</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <!-- Create separate generators artifact for security testing utilities -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generators</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <classifier>generators</classifier>
                            <classesDirectory>${project.build.testOutputDirectory}</classesDirectory>
                            <includes>
                                <include>${generators.classifier.package.generators}/**</include>
                                <include>${generators.classifier.package.database}/**</include>
                            </includes>
                            <excludes>
                                <exclude>${generators.classifier.package.generators}/**/*Test.class</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Guard the generators artifact produced above: a build that skips test compilation
                 (-Dmaven.test.skip=true) leaves testOutputDirectory empty and the generators jar
                 is published with metadata only. Bound unconditionally at verify, which precedes
                 install and deploy, so a defective artifact never reaches a repository.
                 maven-enforcer-plugin is version-managed and already active via cuioss-parent-pom;
                 this only adds an execution, no new plugin and no new dependency. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generators-not-empty</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireFilesExist>
                                    <files>
                                        <file>${project.build.testOutputDirectory}/${generators.classifier.package.generators}</file>
                                        <file>${project.build.testOutputDirectory}/${generators.classifier.package.database}</file>
                                    </files>
                                    <message>The generators/database test classes are missing from ${project.build.testOutputDirectory}, so the 'generators' classifier jar would be published without any classes. Test compilation was skipped (-Dmaven.test.skip=true) — run the build without skipping test compilation.</message>
                                </requireFilesExist>
                                <requireFilesSize>
                                    <minsize>50000</minsize>
                                    <!-- The rule's default maxsize is 10000 bytes, which a healthy
                                         generators jar (~180 KB) exceeds; raise the ceiling so only
                                         the minsize floor is enforced. -->
                                    <maxsize>1073741824</maxsize>
                                    <files>
                                        <file>${project.build.directory}/${project.build.finalName}-generators.jar</file>
                                    </files>
                                    <message>The 'generators' classifier jar is far smaller than a healthy one, so it carries no generator/database classes. Test compilation was skipped (-Dmaven.test.skip=true) — run the build without skipping test compilation.</message>
                                </requireFilesSize>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Surefire useModulePath=false (tests run on the classpath to bypass JPMS module
                 restrictions) is inherited from the parent pluginManagement. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
