# Dogfood test (throwaway)

This file is a non-`.plan` change, paired with a `.plan/**` change in the same diff,
to verify that a mixed diff is reviewed only on its non-`.plan` files. See
cuioss/pr-agent-settings#65, cuioss/cuioss-organization#280, cuioss/coderabbit#5.

Safe to delete; this PR will be closed without merging.
